{
  "name": "cards",
  "description": "example_app",
  "version": "1.0.0",
  "engines": {
    "node": "6.11.1"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run-script build"
  }
}
